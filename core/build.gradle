
dependencies {
    api project(":utils")
}

publishing {
    publications {
        core(MavenPublication) {
            groupId = 'dev.m00nl1ght.clockwork'
            version = rootProject.version
            from components.java
        }
    }
}
