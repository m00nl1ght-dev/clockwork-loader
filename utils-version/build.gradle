
sourceCompatibility = 11
targetCompatibility = 11

publishing {
    publications {
        main(MavenPublication) {
            groupId = 'dev.m00nl1ght.clockwork.utils'
            version = rootProject.version
            artifactId = 'version'
            from components.java
        }
    }
}
